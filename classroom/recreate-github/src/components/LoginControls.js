import React from 'react';

const LoginControls = () => {
    return (
        <section>
            <button>Sign in</button>
            <button>Sign up</button>
        </section>
    );
};

export default LoginControls;